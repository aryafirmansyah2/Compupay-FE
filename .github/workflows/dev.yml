name: 🚀 CI/CD Development

on:
  push:
    branches:
      - dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: Development

    steps:
      - name: 📦 Checkout Code
        uses: actions/checkout@v4

      - name: 🧰 Setup Node.js v20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📥 Install Dependencies
        run: npm install

      - name: 🔐 Docker Login
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: 🛠 Build Docker Image
        run: |
            docker build \
                --build-arg NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_HOST }} \
                --build-arg MEXT_PUBLIC_DEFAULT_THEME=${{ secrets.NEXT_PUBLIC_DEFAULT_THEME }} \
                -t nevmock/n4p-fe-dev:latest .

      - name: 📤 Push Docker Image
        run: docker push nevmock/n4p-fe-dev:latest

      - name: 🚢 Deploy to VPS Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "🔁 Pulling latest Docker image..."
            docker pull nevmock/n4p-fe-dev:latest

            echo "🛑 Stopping and removing old container (if exists)..."
            docker stop n4p-fe-dev || true
            docker rm n4p-fe-dev || true

            echo "🚀 Running new container..."
            docker run -d \
              --name n4p-fe-dev \
              --restart unless-stopped \
              -p "${{ secrets.PORT }}":3000 \
              nevmock/n4p-fe-dev:latest

            echo "🧹 Cleaning up unused Docker resources..."
            docker system prune -f

            echo "✅ Deployment to Development environment completed successfully!"